# GatewayServer 설정
#
# 클라이언트가 아는 유일한 서버. 게임 로직이 없어서 로직 큐도 없고, 값도 이게 전부다.

client_port = 9000

world_host = 127.0.0.1
world_port = 9100

# I/O 스레드만 있다. 하는 일이 바이트 복사와 전달뿐이라 큐를 하나 더 두면 지연만 는다.
io_threads = 2
